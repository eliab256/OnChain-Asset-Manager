import { Json, JsonRpcRequest } from "@metamask/utils";
import { JsonRpcMiddleware } from "./JsonRpcEngine.cjs";
/**
 * Create a middleware function that adds `origin` to the request object.
 *
 * @deprecated Use the v2 `createOriginMiddleware` instead.
 * @param origin - The origin.
 * @returns The middleware.
 */
export declare function createOriginMiddleware(origin: string): JsonRpcMiddleware<JsonRpcRequest, Json>;
//# sourceMappingURL=createOriginMiddleware.d.cts.map